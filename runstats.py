class RunStats:
    def __init__(self, name, insize, outsize, numfaces):

        '''Helps us easily organize our values and call them rather than just having floating
         variables
        '''
        self.name = name
        self.insize = insize
        self.outsize = outsize
        self.numfaces = numfaces

